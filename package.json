{
  "name": "blueprint",
  "version": "0.1.0-140926",
  "private": "true",
  "dependencies": {
    "aws-sdk": "^2.0.17",
    "bunyan": "^1.1.0",
    "lodash": "^2.4.1",
    "moment": "^2.8.3",
    "mongoose": "^3.8.16",
    "mysql": "^2.5.1",
    "nedb": "^0.11.1",
    "q": "^1.0.1",
    "restify": "^2.8.2"
  },
  "devDependencies": {
    "chai": "1.9.1",
    "chai-as-promised": "4.1.1",
    "coffee-script": "1.4.0",
    "mocha": "1.20.1"
  },
  "scripts": {
    "test": "mocha --timeout 20s --compilers coffee:coffee-script -R spec --recursive test/services test/routes",
    "test-services": "mocha --timeout 20s --compilers coffee:coffee-script -R spec --recursive test/services",
    "test-routes": "mocha --timeout 20s --compilers coffee:coffee-script -R spec --recursive test/routes"
  }
}
